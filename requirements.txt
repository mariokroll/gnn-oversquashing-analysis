# ── Deep learning core ────────────────────────────────────────────────────────
# Install torch separately first (see pyproject.toml or README for index URL).
# CPU wheels are the safe default; swap for a CUDA index if you have a GPU.
torch>=2.1.0
torchvision>=0.16.0

# Graph neural networks
torch-geometric>=2.4.0

# Data / ML utilities
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0

# Visualisation
matplotlib>=3.7.0
seaborn>=0.13.0

# Progress bars
tqdm>=4.65.0
